import { TableRow } from "./TableRow";

export default {
  title: "Components/TableRow",
  component: TableRow,
};

export const Default = {
  args: {
    disabled: false,
  },
};

export const Disabled = {
  args: {
    disabled: true,
  },
};